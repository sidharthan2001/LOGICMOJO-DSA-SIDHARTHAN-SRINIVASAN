1) BFS:

class Solution {
    public ArrayList<Integer> bfs(ArrayList<ArrayList<Integer>> adj) {
        // code here
        int V = adj.size();
        boolean[] visited = new boolean[V];
        
        ArrayList<Integer> res = new ArrayList<>();
        
        Queue<Integer> q = new ArrayDeque<>();
        
        q.add(0);
        visited[0] = true;
        
        while(!q.isEmpty()){
            int value = q.poll();
            res.add(value);
            
            for(int neigbour : adj.get(value)){
                if(!visited[neigbour]){
                    visited[neigbour] = true;
                    q.add(neigbour);
                }
            }
            
        }
        
        return res;
    }
}

DFS:

class Solution {
    public ArrayList<Integer> dfs(ArrayList<ArrayList<Integer>> adj) {
        // code here
        ArrayList<Integer> res = new ArrayList<>();
        int V = adj.size();
        boolean[] visited = new boolean[V];
        
        getDfs(0, adj, visited, res);
        
        return res;
    }
    
    public void getDfs(int node, ArrayList<ArrayList<Integer>> adj,  boolean[] visited, ArrayList<Integer> res){
        visited[node] = true;
        res.add(node);
        
        for(int num : adj.get(node)){
            if(!visited[num]){
                getDfs(num, adj, visited, res);
            }
        }
    }
}
